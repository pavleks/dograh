[build]
builder = "DOCKERFILE"
dockerfilePath = "api/Dockerfile"

[deploy]
startCommand = "bash -c './scripts/start_services.sh && tail -f ./logs/latest/*.log'"
restartPolicyType = "ON_FAILURE"
restartPolicyMaxRetries = 10
